# 需要注意

这里记的不是「做过什么」（那在 [CHANGELOG.md](CHANGELOG.md)），而是**踩过、而且以后还会再踩的坑**。

收进来的标准是两条同时成立：

1. **隐蔽**——代码读起来是对的，不看具体数据或不换个环境跑就发现不了；
2. **会重现**——它不是某一处的笔误，而是某类改动的固有陷阱。

每条都标了「现在有没有测试盯着」。**没有测试盯着的那几条只能靠人记得**，
改到相关地方时回来翻一眼。

---

## 一、这个项目特有的错误结构

> **用户无法自己验证核心质量。** 能判断「这个语境够不够推出词义」的人，
> 本来就已经认识这个词了。

这一条是下面很多坑之所以隐蔽的根源。它带来两个推论：

**推论 1：这类错误没有反馈回路。** 不像崩溃或乱码，你永远等不到用户来报。
它不会自我纠正，损害对时间积分无上界。

**推论 2：排查和修复要按「沉默度 × 永久性」排序，而不是按「看起来多严重」。**
在这个项目里**响度和损害是负相关的**——会崩会报错的 bug 自带反馈回路，
总损害被「被发现的时间」截断；用户察觉不到的没有。

这条不是空话。本会话第一轮排查我就是按常规严重度排的（崩溃 P1、数据错 P4），
结果**最沉默、且会写进永久存储的那条排在了最后一批**。
如果当时改到一半停下，交付的就是一个「不再崩了、但仍在安静往库里写错词」的版本——
这个取舍是反的。

---

## 二、模型返回值

### 1. 「合法但形状不对的 JSON」是常态，不是边角情况

`json_schema` **只有 Kimi 会真的执行**，DeepSeek 收下也不生效（见 `schema.py`）。
所以顶层是数组、`words: null`、`strength` 是数字、`title_en` 是对象，
这些都会实际发生。

**为什么隐蔽**：`jsonfix` 保证了「能解析成 Python 对象」，很容易以为这就够了。
但它保证不了「解析出来是这个形状」。形状错误一路带到下游，抛出来的是
`AttributeError`——**而那时整篇文章已经花掉几分钟和上万 token**，
连同已经写好的段落一起作废。本会话一共找到 **10 处**这样的崩溃点。

**什么时候重现**：每新增一个 `llm.json()` 调用点。

**怎么防**：任何 `llm.json()` 的返回值先过 `tasks/article/schema.py` 的
`coerce_*`，不要直接 `.get()`。
✅ 有测试：`test_pipeline.py::test_畸形输出不中断管线`（15 种畸形输入）、
`test_归一函数对任意输入都不抛`

**这条纪律在 `tasks/` 之外同样成立，而恰恰是外面那处被漏掉了。**
`core/health.py` 的 L3 直接写了 `doc.get("sentences")`：模型回一个顶层数组
（上面刚说过这是常态），`AttributeError` 不在那段 `except` 的捕获范围里，
一路冒到接口层就是 HTTP 500。后果比管线里那 10 处更差两级——

- 用户看到的是「检验请求失败」，而不是「任务验收没过、原因是顶层不是对象」。
  **专门用来诊断问题的那一层，在它该报告问题的那一刻崩掉了，等于不存在。**
- L4 排在 L3 后面，于是一次都跑不到。而 L4 是四层里唯一验「审计判得准不准」的，
  也就是这个产品全部价值所在的那一层。

一般化的说法：**校验代码比被校验的代码更不能对输入有假设。** 它的输入
按定义就是「可能不对劲的东西」，这正是它存在的理由。
✅ 有测试：`test_health.py::test_L3_拿到畸形形状时报告而不是崩`、
`test_L3_挂掉不影响最要紧的_L4`

### 1b. 兜底层可以「一直在跑、但一次都没成功过」

`jsonfix` 的第 4 层负责在 `max_tokens` 用尽、JSON 从中间断掉时补齐括号，
把前半段救回来。它有两处都不报错的错，叠加之后**对本项目真正会产出的形状
一次都没救回来过**：

- 回退点用的是 `rfind(",")`，**不区分逗号在不在字符串里**。中英对照的正文里
  逗号遍地都是，找到的几乎总是句子中间的那一个，截出来必然不合法。
- 括号栈是一路扫到**末尾**算出来的，回退却把字符串截回了**更早**的位置。
  被截掉的那几个 `}` `]` 已经在栈里弹过一次，按末尾的栈去补就少补几个。

**为什么隐蔽**：解析失败时调用方会重试，而重试通常能成功。于是
「第 4 层从来没救回来过」和「第 4 层工作正常」在外面看起来完全一样——
差别只是每次截断白烧一次三十秒的调用。而且这一层此前**一条测试都没有**：
它是防线本身，没人给防线写测试。

**什么时候重现**：任何「扫一遍算出状态，再回退到中间某个位置」的修复逻辑。
问一句：**回退之后，那份状态还成立吗？** 以及：在字符串里找结构分隔符，
是不是把正文当成了语法？

**怎么防**：回退点和状态必须取自同一个位置（这里是「记下每个字符串外逗号
当时的栈」）；拿不准该保留多少就给出一组候选，让 `json.loads` 自己裁决，
别在字符串层面猜。
✅ 有测试：`test_jsonfix.py`（48 条；其中
`test_救回来的内容只会是原文的前缀` 扫过每一个截断点，
断言救回来的内容逐字来自模型输出——补齐括号顺手改了内容的话，
得到的是一篇「看着正常但不是模型写的」文章，那比解析失败严重得多）

### 1c. 守住了容器的形状，值的形状还是敞着的

第 1 条那一轮把 `health.py` 的**容器**守住了（顶层不是对象、`sentences`
里躺着字符串），值没有：`{"sentences": [{"en": {"text": "..."}}]}` 这种
——`en` 是个对象——照样一路走到 `(s.get("en") or "").strip()`，抛 `AttributeError`。
后果和第 1 条一字不差：四层检验整个变成 HTTP 500，用户看到「检验请求失败」，
而**最要紧的 L4 一次都跑不到**。

同一类还有一处更隐蔽的：`list_models()` 声明返回 `list[str]`，实现却是
`m.get("id", "")`——响应里混进一个数字 id，L1 拼那句「近似可用：……」时
`"、".join(...)` 抛的是 `TypeError`，同样没人接。

**为什么隐蔽**：修上一轮时注意力全在「形状」两个字的容器含义上，
而 `tasks/article/schema.py` 的 `_text` 早就把「值不是字符串」当成常态处理了
（注释里写着「模型偶尔把 title_en 写成对象」）。**防线是有的，只是
`health.py` 另起了一套取值方式，没跟过来**——和第 3b 条是同一个形状的错：
约定要在**每一个**出口守住，而不是在想得起来的那几个。

**什么时候重现**：任何一处不经过 `coerce_*` 就直接读模型返回值的代码。
问一句：**这个值除了「有没有」，我还假设了它是什么类型吗？**

**怎么防**：取值也走一个统一入口（`health._field`，非字符串一律当空），
和 `schema._text` 是同一条规矩；返回 `list[str]` 的函数就在出口处滤掉非字符串。
✅ 有测试：`test_health.py::test_L3_拿到畸形形状时报告而不是崩`（新增 5 条值畸形）、
`test_值不是字符串时_L4_照样跑得到`、`test_取值对任意类型都不抛`、
`test_provider.py::test_模型_id_不是字符串时不漏出_TypeError`

### 2. 兜底不能把「崩溃」变成「静默的成功」

给上一条做归一之后出现了新问题：模型全程返回垃圾时，管线不再崩了，
而是**安安静静存下一篇 0 词的文章**，界面显示「生成完成」。

**这比报错更糟。** 修兜底时要一起想：兜完之后如果结果其实不可用，
有没有人会发现。

✅ 有测试：`test_pipeline.py::test_全线返回_null_时报可读错误`

### 2b. 同一条规矩，第 5 层守了，第 4 层没守

`jsonfix` 第 5 层写着「空结果不算修好」，并且明确引用了上一条的理由。
第 4 层（截断补齐）没有这道闸，于是它自己就在造上一条要拦的东西：

| 截断在 | 补出来 |
|---|---|
| `{` | `{}` |
| `{"sentences": [` | `{"sentences": []}` |
| `{"sentences":[{` | `{"sentences": [{}]}` ← 还凭空多造了一句 |

最后一行直接违反第 4 层自己那句注释：「宁可少一句，也不要凭空多一句模型没写的」。

**为什么隐蔽**：下游确实兜得住——空段落会被 `check_paragraph` 判成 `empty`，
最后还有一道「一个字都没写出来就报错」。所以它不会崩，只会**多花钱**：
本该由 `client.py` 便宜地重试一次的事，变成拿修复预算去修一段空文章。
审计那条路更糟——`{"audits": []}` 被 `audit_clues` 读成「所有词都没有线索」，
一段本来合格的文章要挨两轮补线索改写，还可能被改坏，
而界面上只显示一句「第 N 段线索不足」，看着完全像正常运转。

**什么时候重现**：任何有多层兜底的地方。问一句：**上一层立的规矩，
这一层守了吗？** 规矩写在注释里而不是代码里时，答案通常是没有。

**怎么防**：把规矩落成一个共用的判据（`jsonfix._hollow`），而不是每层各写各的。
✅ 有测试：`test_jsonfix.py::test_补出来的空壳不算救回来`、
`test_救回了内容就照常返回`（后者盯着这道闸别把救到一点的也扔了）

### 2c. 归一了形状，没归一「数量」

管线里每一处模型输出都做了收敛：`coerce_*` 管形状，`_assign_words` 管词的
归属，`coerce_audits` 管强度取值。唯独**段数**是照单全收的——`MAX_PARAGRAPHS`
从头到尾只是 `sizing()` 的入参，从没拦过模型真的回了几段。

实测模型回 40 段：顺风路径的 4 次调用变成 **44 次**，按本机 36.1 秒/次算
二十多分钟。

**为什么隐蔽**：不报错，而且前端那条进度条**专门设计成会自己把分母加大**
（免得卡在 99% 被读成死了）。于是超出预算的表现和「这篇比较长」完全一样。

**什么时候重现**：任何「按模型返回的列表逐项再调一次模型」的地方。
问一句：**这个列表的长度是谁决定的？**

**怎么防**：在入口就截断，并且**说一声**——悄悄截断读起来和「模型本来就只
规划了这么多段」一模一样。截断是安全的：被砍段落里的目标词会被
`_assign_words` 当成 forgotten 补回最后一段。
✅ 有测试：`test_pipeline.py::test_模型回多少段就写多少段是不行的`、
`test_没超上限时不多嘴`

### 2d. 同一个语义判断，两处用了两套判据

`audit_clues` 把模型回的结论认领到目标词上，用的是 `lemma` 字符串相等；
而 `_appears` 和 `cefr.scan` 判「是不是同一个词」用的是 `same_word`。
问它 meticulous、它回 meticulously，字符串就对不上了。

后果是连锁的，而且每一环都不报错：
1. 这个词被当成「漏审」，按最坏情况兜底成 `none`；
2. 一段本来线索充分的段落挨两轮补线索改写——4 次多余调用、两分多钟，
   而改写只可能让它变差；
3. 结果面板把它报成「语境线索充分 **0/1**」——**正好反了**。

第 3 条最要命：`SENTENCE_SCAFFOLD` 那段注释敢按低值发，理由是
「这个风险有仪表，错了会响」。仪表读反了，那条前提就不成立了。

**什么时候重现**：任何拿模型回声去匹配自己这边某个 key 的地方。
问一句：**这个项目对「同一个东西」已经有判据了吗？**（这里有：`same_word`。）

**怎么防**：认领分两遍——先让字面相同的认完，再让屈折形认剩下的；
认领过的从池子里拿走，一条结论只能算到一个词头上，不然分母会虚高。
✅ 有测试：`test_pipeline.py::test_模型回了屈折形也认得出是同一个词`、
`test_一条结论只能算到一个词头上`、`test_完全对不上的结论仍按最坏情况处理`

### 2e. 收敛了「哪些词要学」，没收敛「哪些词被标成了 target」

`_assign_words` 的注释里已经写着「模型有两个稳定的跑偏方向」，其中第一个就是
**自己往里加词**。那一层确实拦住了——拦的是**选题结果**。而 `_normalize`
处理模型在正文里标的 `targets` 时，是照单全收的，于是同一个跑偏方向从另一个
口子进来了，而且这个口子直通 `data/app.db`：

· 让它写 abandon / silence，它顺手把 `shop`、`spring` 也标成 target。这两个词
  拿到自己的 `Word` 行和 `Encounter`，进词库、算进「累计词条」和
  「在多个语境中见过」——**这个应用用来说明自己有用的那两个数字**。
· 问它 abandon，它把 lemma 标成 `abandoned`。而 abandoned 自己就是 B2 词条，
  `resolve()` 归不回 abandon，于是库里同时立着两条，同一个词的语境从此
  分摊在两个词条下（第 6b 条那个裂缝，这里是它的另一个入口）。
  连带着 `record_form` 也不会把 abandoned 挂成 abandon 的形态、释义和线索
  也挂不上——三样东西一起挂到了一个错的词条上。

**为什么隐蔽**：文章本身是好的，校验全过，界面显示「生成完成」。
错误只在词库页显示为「多了几个我不记得学过的词」，而那正好会被读成
「哦大概是之前哪篇里的」。**而且写进去就拿不出来了**——事后没有任何办法
把它们和真正学过的词分开。按「沉默度 × 永久性」排，这条两项都拉满。

**什么时候重现**：任何拿模型返回的标识去建库、或者去 key 一张表的地方。
问一句：**这个标识是模型说了算，还是用户说了算？**

**怎么防**：把模型标的 lemma 认领到用户这次要学的词上（先比字面、再比
`same_word`），认不上就丢掉，并且归到用户给的那个拼写。判据用 wanted
（**全篇**的目标词）而不是 expected（本段的）——一个分给第 3 段的词真的
出现在第 1 段里，那是一处货真价实的语境，不该丢。
丢掉的那些不必报给用户：用户没要求学它们，丢了什么也没少。
✅ 有测试：`test_pipeline.py::test_模型自己加的词不许写进词库`、
`test_模型把_lemma_回成派生形式也要归回原词`、
`test_别的段落分到的词出现在本段也算数`、
`test_同一句里同一个词的两种形态都要留着`、
`test_认领之后撞车的两条要去重`、`test_收敛不影响模型漏标时的兜底`

### 3. 声明了 quirk 却没有任何代码读它

`providers.yaml` 里 `may_return_empty`、`sampling_ignored_when_thinking` 之类，
解析进了 dataclass，但调用方从没按它分支过。README 却写着
「调用方按声明分支，而不是靠试错」。

**为什么隐蔽**：配置看着很完备，`grep` 也能搜到名字（在定义和解析处），
只有搜「谁读它」才发现没人读。

**什么时候重现**：每新增一个 capability / quirk 字段。

**怎么防**：加字段时同步加一处「读它并据此分支」的代码，或者干脆别加。
❌ 没有测试盯着。

### 3b. 抽象层的约定，要在每一个出口都守住

`ProviderError` 的注释写着「带上下文的调用失败，便于在设置页里显示人话」，
`_explain()` 也确实把每个状态码翻成了中文。但 `r.json()` 那一行不在这套翻译里：
`base_url` 指到网页、代理或另一种协议上时，拿回来常常是一个 **200 的 HTML 页**，
抛的是 `ValueError`，谁都没接，一路冒到接口层就是 HTTP 500。

**为什么隐蔽**：这一层看起来处理得很周全——异常有、状态码有、错误文案也有，
唯独漏的那一行长得完全不像错误处理。而且它只在「配置指错了」时触发，
恰恰是最需要它说人话的场合（见第 1 条：校验代码比被校验的代码更不能对输入有假设）。

**什么时候重现**：每新增一个走 HTTP 的方法，或者每次直接对响应体 `.get()`。

**怎么防**：响应体统一走一个入口（`OpenAICompatProvider._body`），
在那里把「不是 JSON」和「顶层不是对象」都翻成 `ProviderError`。
✅ 有测试：`test_provider.py`（`200 但返回网页`、`顶层不是对象`、
`合法但形状不对的响应不抛`）

---

## 三、词表与词形

### 3c. 借了一半的机制：标记有了，后果没有

`STATUS_IGNORED = 99「忽略（专有名词等）」`——字段有、阅读页按 `I` 就能标、
词库页也显示。**但难度标尺从来不读它。** 把 Nora 标成忽略，下一篇生成完全
不受影响，那个按键只是个标签。

这是第 3 条的一个变体，而且更隐蔽：第 3 条是「声明了却没人读」，
这条是**整条链路看起来都通了**——模型层、接口、界面、快捷键、状态显示，
一个不缺，唯独最后那一步没接上。而且它不会报错，只会让用户觉得
「我标了怎么没用」，然后不再标。

**为什么隐蔽**：`grep STATUS_IGNORED` 能搜到六处，看着很完备；
只有搜「谁按它做判断」才发现一处都没有。

**什么时候重现**：任何「从别的项目借一个概念」的时候。
问一句：**这个状态改变了什么？** 答不上来就是只借了个名字。

**怎么防**：借概念时连它的**后果**一起借。Lute 那边的后果是判定依据：
算生词占比时只把 `status 0`（人从没碰过）算成生词，凡是人碰过一次的
（含 99）就永远不再计入（`lute/book/stats.py::calc_status_distribution`）。
现在这里也一样：标过忽略的词由接口层查出来、当参数传进管线，
和目标词合成同一份豁免集合喂给 `cefr.scan`。

顺带一提为什么是「让人标」而不是「自动认」：专有名词识别没有便宜又可靠的
判据，人名和普通词大面积同形（实测 4945 个英文名和 CEFR 词表撞 90 个：
Rose、Will、Grace、Hope、Faith、June…）。这个领域的两种成熟做法都是
**做成数据、别去推断**——Paul Nation 的 BNC/COCA 词表随词表附一张专有名词表，
Lute 让用户标一次然后永久生效。
✅ 有测试：`test_store.py::test_标成忽略的词能查出来`、
`test_pipeline.py::test_标成忽略的词不再算超纲`、`test_忽略集合脏了也不出错`、
`test_web.py::test_生成时把标过忽略的词带下去`

### 4. 「大写就跳过」会连每句的第一个词一起跳掉

原本是为了跳过专有名词。副作用是**模型只要把生词放在句首，就能绕开整把难度标尺**。

**为什么隐蔽**：这个启发式读起来完全合理，而且它确实解决了专有名词的问题。
要发现它得专门构造「同一个词分别放句首和句中」来对比。

**什么时候重现**：任何时候有人再加「跳过专有名词」的逻辑。

**正确的方向不是把启发式磨得更准，而是别再猜**——歧义之所以存在，
是因为把生成阶段本来就有的信息扔了。现在 plan 会声明 `names`（人名地名），
句首词因此可以无条件检测；模型漏报时还有一层不依赖它的实据：
同一个词在别处以大写出现在句中。

✅ 有测试：`test_lexicon.py::test_句首生词不能逃过检测`、`test_句首与句中判定一致`、
`test_声明了人名就能无条件检测句首词`、`test_没声明人名时靠句中大写兜底`

### 4b. 兜底值的方向：认不出来时该往严还是往松

`within()` 里是 `LEVEL_INDEX.get(max_level, len(LEVELS) - 1)`——查不到就取
**最后一档**，也就是最宽松的 C2。于是一个 `b2`、一个尾随空格、或者
`settings.local.json` 里留下的一个旧值，就能让整把难度标尺静默失效：

```
传 'B2' → 判出 13 个超纲词        传 'b2' / 'B2 ' / '' / 'Z9' → 11 个
```

界面上写着 B2，实际按 C2 放行。**「界面说的」和「实际拦的」不一致，
正是这个项目里用户没法自己发现的那一类**——能判断「这篇的用词是不是超了 B2」
的人，本来就不需要这个功能。

**什么时候重现**：任何 `dict.get(key, 默认值)` 里的默认值代表某种「宽严」时。
问一句：**这个默认值是往安全的方向倒，还是往「什么都放过」的方向倒？**

**怎么防**：两头都做。入口处 `normalize_level()` 收敛成已知的六档之一
（接口层非法值直接 400，和 `active_provider` 同等对待）；`within()` 的
兜底改成**最严**那一档——正常走不到，真走到了宁可整段判超纲、让修复循环
当场炸出来，也不要安安静静把标尺放到最宽。
✅ 有测试：`test_lexicon.py::test_用词上限收敛到已知的那六档`、
`test_认不出来的上限按最严算`、`test_web.py::test_设置接口拒绝认不出来的用词上限`

### 4c. 一个洞可能正在替另一个洞遮丑——先量再改

选题阶段模型自己申报的 `names`，`cefr.scan` 是无条件采信的（只校验了是非空
字符串）。而 prompt 里那句「漏报了，你的角色名会被当成超纲词退回来重写」
是**单边施压**：多报没有代价，少报要挨一次重写。模型自然会多报。
构造一下确实能把超纲率从 26% 压到 7%。

看着该收紧。但先量了本机 7 篇真实文章——把 `names` 一律不信重算：

| | 存下的 | 不信 names |
|---|---|---|
| The Interview | 1.3% | 1.8% |
| Piles of Trouble | 1.0% | 1.4% |

差额来自 `You're`、`Standing` 这类词，**不是被藏起来的难词**。再往下查：

- `You're` → `resolve` 得到 `you're`，词表里没有 → 判超纲。可 you are 是 A1。
- `Standing` → `level_of` 取的是**第一个命中候选**，而 `standing` 自己是 C2 词条
  （名词「地位」），于是 A1 的 `stand` 根本轮不到。
- `cones` → `resolve` 归到了 `con`（C1），不是 `cone`；`rehearsed` 归到 `rehears`。

也就是说：**`names` 一直在替标尺自己的误判遮丑。** 只收紧 `names`，
这些误判会立刻变成 `too_hard`，拿修复预算去要求模型「把 Standing 换成
B2 以内的说法」——花钱把一段本来合格的文章改坏。实测收紧后 7 篇里
多触发 1 次修复，而那一次正是误判引起的。

**教训不是「这个洞不用修」，是「改之前先量，否则你修的可能不是承重的那一处」。**
真正承重的是 `level_of` 的候选取法：`_load()` 里早就写了「同一个词有多条词性
记录时取最容易的那一级」，而跨候选（`standing` vs `stand`）却取了第一个。
两处该是同一条规矩。

**后来把承重的那处修了**：`level_of` 改成在候选里取**等级最容易的那一档**，
和 `_load()` 里「同一个词有多条词性记录时取最容易的那一级」同一条规矩。
改动是**单向放松**的（任何词只可能变容易），所以风险全在「会不会放得太宽」
一侧，量出来是：

- 8653 条词表里 518 条判定变松，71 条越过 B2 线；
- 其中 67 条是对的——`reluctantly ← reluctant`、`standing ← stand`、
  `trying ← try`、`revealing ← reveal`，全是叙事文里的高频词；
- 3–4 条是过度还原：`batter ← bat`、`charter ← chart`、`wares ← war`、
  `flatter ← flat`（都是 `-er` / `-es` 规则剥过头），都是低频词。
- 本机 7 篇真实文章：超纲词 10 → 8，触发 `too_hard` 的段 1 → 0，
  **没有任何词新增被判超纲**。

高频的误判换低频的漏判，划算。残留那三四个记在这里，别以为标尺现在是准的。

❌ 仍未修：`names` 的无条件采信。上面这一层堵掉之后它遮的丑少了，
但那个口子还在——而且没有已知的、代价可接受的判据能关上它。
✅ 有测试：`test_lexicon.py::test_等级取最容易的那个候选`、
`test_取最容易的候选只会放松不会收紧`、`test_并列时仍然以词本身为准`

### 5. 「是不是同一个词」这个判断是有方向的

`better` 的词典父词条确实是 `good`。但拿「两边指向同一个词根」来判，
文中出现 `good` 就会被判成「目标词 better 已出现」——**校验被骗过去，
`good` 还会被当成 `better` 的形态写进词条库**，词条面板里就出现一句
根本没有那个词的例句。

正确的判据只认两种情况：token 能还原到 target（`ran` → `run`），
或者 target 规则地屈折成 token（`studies` → `study`）。
反过来把 target 往上还原一层，就会把整张 `IRREGULAR` 表变成误报源。

**为什么隐蔽**：`ran`/`went`/`children` 全都正常，只有异干替补那十几个词出错，
而它们不是典型的学习目标，不特意构造就碰不到。修之前**全词表里有 47 个词**
会把父词条误判成自己。

**什么时候重现**：任何「这两个词算不算同一个」的判断——搜索、去重、统计。

✅ 有测试：`test_lexicon.py::test_不把别的词判成目标词`、`test_异干替补不算同一个词`、
`test_真正的不规则形态必须保留`

### 6. 同一个函数被两个地方用，但它们要的不是一回事

`cefr.resolve()` 同时服务「查这个词的难度等级」和「这个词条该归到谁名下」。
前者需要顺着父词条走，后者不需要——结果是用户说要学 `people`，
词条面板标着 `person`。

修 `_appears` 时我只改了匹配那一侧，**存储那一侧留在了旧规则上**，
两处从此用不同的判据。

**什么时候重现**：任何一个「看起来通用」的工具函数被第二个调用方接上时。
问一句：这两个调用方要的真是同一件事吗？

✅ 有测试：`test_store.py::test_词条按用户给的词存不改名`

---

### 6b. 「两边还原到同一个词」不等于「两边碰得上头」

超纲检测放行目标词，用的是「`resolve(文中的词)` 是不是等于 `resolve(目标词)`」。
这条在 `abandon` / `abandoned` 上不成立——**因为 `abandoned` 自己就是一个
CEFR 词条**（B2，比原形 `abandon` 还高一级），`resolve` 走到它就停了，
根本不会再往 `abandon` 还原。词表里这样的组合有 **983 对，其中 496 对
派生形式更难**，也就是恰好会顶破用词上限的那些。`-ly` 副词、`-ed` 分词、
`-ness` / `-tion` 名词化，词典都会单独收，所以这不是边角情况。

**为什么隐蔽**：`resolve` 读起来完全对，`silence`/`silenced`、`confess`/`confessed`
这些也确实是对的——只有当派生形式**恰好也在词表里**时才断链，
而那取决于数据而不是代码。更要命的是它错的方向：错误的结论不是
「漏判了一个超纲词」，而是**把目标词本身判成超纲词**，然后拿修复预算
要求模型「把 abandoned 换成 B2 以内的说法」——花钱让它删掉这篇文章
要教的那个词。下一轮校验再报「目标词没有出现」，两条指令互相打架，
`MAX_REPAIRS` 全烧光。全程零报错，用户看到的是「生成完成」。

**什么时候重现**：任何一处「这个词算不算目标词」的判断。它和第 5 条是同一枚
硬币的两面：第 5 条是判得太松（`better` 把 `good` 认成自己），这条是判得太紧。

**怎么防**：这个项目对「这是不是同一个词的另一种形态」只有一个判据，
就是 `lemma.same_word`（有方向的那个），`_appears` 用的就是它。
**问同一个问题的地方必须用同一个判据**——一边说「目标词出现了」、
一边说「这个词超纲」，就是两处用了两套判据。
**后来又堵了一层**（见第 4c 条）：`level_of` 现在取**最容易的候选**，
所以 `abandoned` 直接按 `abandon` 的 B1 算，压根不会被判成超纲——
根因在标尺那一侧也堵掉了。`allow` + `same_word` 退成第二道防线，
只在原形自己也超纲、或者压根不在词表里时才用得上。
两道防线要分开测：合在一条里的话，标尺那边一改，这条就变成空跑了（第 17 条）。
✅ 有测试：`test_lexicon.py::test_派生形式按原形的难度算`（用注入的小词表，
不依赖机器上有没有下载 CEFR-J）、`test_原形本身也超纲时靠_allow_兜住`、
`test_目标词不会把别的词一起放行`、`test_放行判据和目标词命中判据是同一个`、
`test_pipeline.py::test_不会拿修复预算去删掉目标词本身`

### 6c. 边界条件写宽一格，受害的是最常见的那批词

`-ed` / `-ing` 的还原门槛原先是 `len(w) > len(suf) + 2`，即「去掉后缀还剩
三个字母」。剩两个字母就够了，多要的那一格恰好卡掉了
`used` / `aged` / `owed` / `tied`（4 字母）和 `using` / `going` / `dying`（5 字母）
——**英语里最高频的一批词形**。

**为什么隐蔽**：`+2` 看着像个合理的保守值，而且它对 `stopped`、`studied`、
`running` 这些「典型例子」全都工作正常。测试也是拿典型例子写的。
出问题的是短词，而短词恰恰是最常用、最不会被拿来当测试样例的那些。

**什么时候重现**：任何一处 `len(x) > 常数` 的守卫。问一句：**这个常数排除掉的
到底是哪些输入？** 把它们列出来看一眼，而不是只看它放行的那些。

**怎么防**：守卫的常数要从「结果必须满足什么」倒推——这里是「词干至少两个
字母」，所以是 `+1` 而不是 `+2`；不要凭手感取一个「看着安全」的值。
拿 LemmInflect 的 13731 个真实屈折形态当基准量一下，比读代码可靠。
✅ 有测试：`test_lexicon.py::test_四五个字母的规则变形也认得出`（8 组）、
`test_目标词的短变形不算超纲`

## 四、只在特定环境下暴露的

### 7. `pythonw` 下 `sys.stdout` 是 `None`

没有控制台，`sys.stdout` / `sys.stderr` 都是 `None`。任何 `print`、
任何往 stdout 装 handler 的库都会当场炸——**uvicorn 就是其中之一**。

**为什么隐蔽三重**：
- 用 `python` 跑完全正常，只有 `pythonw` 才炸；
- **而 `pythonw` 曾经正是双击 `run.bat` 的真实路径**；
- 异常死在后台线程里没人接，表现出来只是「端口一直连不上」，
  真正的原因被完全盖住。

**现状**：这条路径已经没有了——`run.bat` 改成前台跑 `main.py`，控制台一直开着。
条目留着不是历史记录，是因为**触发它的动机随时会回来**：那个黑窗口不好看，
迟早有人想用 `pythonw` 或 `start /min` 把它藏起来。

**什么时候重现**：① 任何把启动改成无控制台的改动；
② 在无控制台前提下，任何新增的、会往 stdout 写东西的依赖。

**怎么防**：真要藏窗口的话，进程开头就得把 `sys.stdout/stderr` 兜到空设备
（必须在 import 那些库之前），后台线程的异常要收集到主线程报出来。
而且**测启动一定要按真实方式测**（双击 `run.bat`），不能只测 `python main.py`。
更省事的做法是别藏——顺带还解决了「用户怎么关掉它」。
❌ 没有测试盯着（要真启进程才验得了）。

### 8. 批处理里 `%` 是变量展开符

往 `run.bat` 里写 Python 单行脚本时，`'%d 份快照' % (n,)` 里的 `%d`
会被 cmd 吃掉，跑出来是 `TypeError`。

**怎么防**：`.bat` 里的内联脚本一律用字符串拼接，不出现 `%`。
现在 `run.bat` 里 `%d`/`%s` 的数量是 0。
❌ 没有测试盯着。

### 8b. `cd` 失败不会让批处理停下来

`run.bat` 第 2 行是 `setlocal EnableDelayedExpansion`，第 4 行是
`cd /d "%~dp0"`。**路径里有感叹号时，感叹号会被延迟展开吃掉，`cd` 直接失败**
——而 `cd` 失败不中断批处理，它会接着在**原来的目录**里跑。实测：

```
'plain'        -> CWD=[...\plain]   FOUND-PROJECT-FILES
'danci!xuexi'  -> CWD=[C:\Windows]  WRONG-DIRECTORY
```

双击时那通常是 System32，于是后面会往那里建 `.venv`、找不到
`requirements.txt`，报出来的错和真正的原因（路径）毫无关系。
同一个根因还盖住另一种情况：从网络共享运行（`\服务器\共享`），
cmd 不支持把 UNC 当工作目录。

**为什么隐蔽**：开发机的路径永远是好的。这条只在**别人的机器上**触发，
而那时没有控制台历史、只有一句看不懂的报错。

**什么时候重现**：任何依赖 `cd` 成功的脚本。（第 8 条讲的是 `%`，
这条是 `!`，同一类。）

**怎么防**：`cd` 之后立刻拿一个必然存在的项目文件验一下，不对就
`pause` + `exit /b 1`，把原因直接说出来。比逐个去猜路径里有什么字符可靠。
✅ 有测试：`test_launcher.py::test_切目录之后立刻验一下切成功了没有`
（结构性断言，防的是「哪天有人顺手删掉」）

### 9. `.bat` 必须是 CRLF

cmd 对 LF 换行的处理不一致，`goto`/标签可能解析出错——克隆下来直接跑不起来。
仓库里已用 `.gitattributes` 把 `*.bat` 钉死成 CRLF。

**什么时候重现**：任何在类 Unix 环境（含 WSL、Git Bash）里重写 `.bat` 的时候。

---

## 五、计数与状态

### 10. 加法有人写，减法没人写

`times_seen` 是一次次加出来的，删文章时却没人减。结果：词条面板显示
「见过 3 次」却只列得出 1 处语境，文库页的「在多个语境中见过」永久虚高——
**而那正是这个应用用来说明自己有用的那个指标**。

**为什么隐蔽**：删除路径本身工作正常，文章确实没了；错的只是一个数字，
而没有任何地方会去校验它和事实一致。

**什么时候重现**：任何新的「累计」字段，以及任何新增的删除路径。

**怎么防**：优先**按事实重算**而不是做减法——减法要求每条删除路径都记得配一次，
重算只依赖删完之后的状态，以后再多几条删除路径也不会重新错。

✅ 有测试：`test_store.py::test_删文章后计数按事实重算`（断言 `times_seen == len(contexts)`）

### 10b. 修好原因不等于修好数据

上面那条改完，测试全绿，代码也确实是对的了——**但库里已经错掉的那些数字
一个都没变**。重算只在该词条又被别的删除路径碰到时才发生，碰不到就一直错下去。
开发机的库里 24 个词条有 5 个对不上，`abandon` 显示「见过 10 次」、
实际只列得出 3 处语境。

**为什么隐蔽**：修完之后所有注意力都在「以后不会再错了」上，而验证手段
（跑测试）用的是当场造的干净数据，永远看不见存量。

**什么时候重现**：任何一次「从增量维护改成按事实重算」的修复。
问一句：**已经存下的错值，谁来修？**

**怎么防**：这类值既然是可以从事实推出来的，就在启动时校一次
（`db._reconcile_counts()`）。只写对不上的行，库干净时是零写入。
备份在它之前跑（`init_db` 第一步），出事退得回去。

✅ 有测试：`test_store.py::test_启动时把虚高的计数校回事实`、
`test_老库补出来的列是空的也能校回`、`test_库是干净的时候一行都不写`

### 10c. 安全网恰好在它该起作用的那一次失效

`backup.run()` 有条短路：**库自上次备份后没被写过，就不重复留档**——
一天开十次应用不该攒出十份一模一样的快照。这条本身是对的。

坏就坏在它和第 10b 条的启动校正撞在一起：「库自上次备份后没被写过」
正是「这次启动才第一次要动它」的典型场景。于是**唯一一次真正改数据的启动，
反而没有当次快照兜底**。本会话就实际走到了这一步——真实库被校正了，
而当次备份被跳过；改前的状态只是因为碰巧还留着一份更早的快照才没丢。

**为什么隐蔽**：备份代码是对的，校正代码是对的，各自的测试也都过。
错的是两者的**顺序与前提**：一个的「不必做」恰好等于另一个的「必须做」。
而且它只在「库没被写过」这一支上发生，随手测一下（造点数据再跑）永远碰不到——
造数据这个动作本身就把前提破坏了。

**什么时候重现**：任何「平时跳过、必要时才做」的保护措施，
接上一个「平时不动、必要时才写」的操作。
问一句：**这两个「必要时」是不是同一时刻的反面？**

**怎么防**：要写库的调用方自己负责留档——先只读地数一遍
（`_count_stale()`），确认真要动了，再 `backup.run(force=True)`，最后才写。
✅ 有测试：`test_store.py::test_校正之前一定留得下改前的快照`
（先把库置成「备份比库新」，断言普通备份此时确实会跳过，再断言校正前多留了一份）

### 10d. 两份不可再生的状态，只有一份被当回事

`data/app.db` 有启动快照、有 `.partial` + 原子改名、有「动 schema 之前先留档」。
`config/settings.local.json` 装的是 API Key，同样不可再生，而它一直是
`write_text` 直接写——**先把文件截断，再往里写**。

断电 / 进程被杀 / 磁盘满卡在这两步之间，留下的是半个 JSON。接下来：

1. `_read()` 把 `JSONDecodeError` 吞掉，安静地退回 `DEFAULTS`——Key 没了，
   provider 也回到了默认那家；
2. 用户随手再存一次设置，那份不带 key 的 `DEFAULTS` 被完整写上去，
   连「半个文件」这条线索也没了。

**为什么隐蔽**：这个仓库里已经有一份写得很讲究的原子写（`backup._snapshot`），
就在二十行外。**有防线不等于防线覆盖到了每一份该保护的东西**——
盘点的时候容易只数「被保护的那些」，不数「同类里还有谁」。

**什么时候重现**：任何新增一份「丢了补不回来」的本地状态时。
问一句：**这个仓库里同类的东西是怎么写的？**

**怎么防**：同盘 `.partial` + `replace()`，和数据库那边同一个写法。
✅ 有测试：`test_settings.py::test_写设置是原子的`、`test_不留下临时文件`、
`test_半个文件不会被当成合法设置读进来`

### 10e. 最不可逆的那个动作，阻力最小

文库页的「删除」是一次点击就生效的，按钮平时还是隐形的（`.row-act` 平时
`opacity: 0`，悬停才出现）。而删一篇文章会**连着它攒下的语境一起删**——
`Encounter` 跟着 `Sentence` 级联走——正是 CLAUDE.md 里那句「文章能重新生成，
掌握程度和累计语境不能」说的那份东西。

安全网也没接住：例行快照的窗口是「**最近 KEEP 次有写入的启动**」，
不是「最近几天」。误删之后再生成 5 篇，删之前那一份就被轮换掉了，
而那正是唯一想找回来的一份。

**为什么隐蔽**：删除本身是用户主动点的，不算 bug；代价却是不可见的——
界面从没说过它会带走语境，删完之后连「本来有多少」都查不到了。
按「沉默度 × 永久性」排，永久性拉满。

**什么时候重现**：任何新增的删除路径；任何「保留最近 N 份」的轮换策略。
问一句：**这个 N 是按什么计的？** 按次数计的窗口，在事情最密集的时候最短，
而事情密集恰恰是最容易出错的时候。

**怎么防**：三件事一起做，缺一件都不够——

1. 二次确认，而且重点不是「你确定吗」，是**把代价摆出来**：多少处语境、
   哪几个词只在这一篇里出现过（`db.deletion_impact`）。
2. 动手之前强制留档，放在 `delete_article` **里面**而不是接口层——
   「每条删除路径都记得配一次」这种要求迟早会漏，和这个函数选择重算
   而不是做减法是同一个理由。
3. 这份快照走**单独的轮换线**（`backup.BEFORE_DELETE`），例行快照再多也
   挤不掉它。否则第 2 条会被第 1 条之外的日常使用悄悄抵消。

留档失败要如实说（接口把结果回给前端）：**备份悄悄失败比没有备份更糟**，
用户会以为自己还有退路。
✅ 有测试：`test_store.py::test_删之前先算清楚要丢什么`、
`test_删之前一定留得下删除前的快照`、`test_例行快照挤不掉删除前那一份`、
`test_web.py::test_删除如实说留没留下快照`、
`dom.test.mjs`「第一下不删 / 取消 / Esc / 算不出代价也能删」

### 11. 按时间戳命名的轮换会因撞名而静默失效

备份文件名只精确到秒，同一秒内的两次备份撞上同一个名字，
`replace()` 直接把前一份盖掉——**轮换看着在跑，实际一份都没多**。

**什么时候重现**：任何按时间戳命名 + 保留最近 N 份的机制。

**怎么防**：精度要够（现在到毫秒），而且位宽固定放在末尾，
好让「逆字典序 == 逆时间序」继续成立（`snapshots()` 依赖这一点）。

✅ 有测试：`test_backup.py::test_轮换只留最近若干份`（同一秒内连做 14 次）

### 12. 统计口径：算的是「尝试了几次」还是「生效了几次」

`clue_fixes` 原本在「这次改写是否被采纳」判断之前就自增了，
被丢弃的改写也算进去，数字对不上文章的实际内容。

**什么时候重现**：任何「尝试 → 可能回滚」的循环里的计数。

---

## 六、界面

### 12b. 算出来了、但没在界面上编码，等于没算

CEFR 等级、线索强度、掌握程度、见过几次——这四样都是花了代价算出来的
（`cefr.scan`、那次专门的线索审计调用、每次阅读的累计），可界面上它们
全长一个样：六个 CEFR 等级是同一个灰药丸，线索比是一个裸的 `3/5`，
和旁边的「178 词」一样重。**扫一眼看不出任何结论，得逐格去读。**

**为什么隐蔽**：功能是好的，数据是对的，测试也全绿——jsdom 验的是
「这个格子里有没有那个数」，而它确实有。没有任何自动化手段会说
「这个数字没被编码成颜色或形状」。而用户也不会来报：界面「能用」。

**什么时候重现**：每新增一个有序的数据维度（等级、强度、频次、进度）。
问一句：**这个维度是看出来的，还是读出来的？**

**怎么防**：有序维度配色阶（`tokens.css` 的 `--lv-a1..c2`），
比例配分段条（`.clue-bar`），频次配点（`.count`），三选一。
❌ 没有测试盯着（jsdom 量不了，见第 16 条）。

### 12c. 算出来了、但两个分支返回同一个值，等于没算

生成页那条「第 N 段完成」原先是这么写的：

```js
const weak = audits.some((a) => a.strength !== 'strong');
ui.tl.line(`第 ${ev.index} 段完成`, detail, weak ? '' : '');   // ← 两边一样
```

`weak` 算出来了，可三元的两个分支返回的是同一个空串。于是「补线索两轮都没
救回来、这一段里有词读完猜不出意思」和「全部 strong」在时间线上长得一模一样。
这是第 12b 条的一个更彻底的版本：那条是「算了但没编码成视觉」，这条是
**算了但根本没接上**。

同一类还有一处纯 CSS 的：`app.css` 里 `.meter` 写了两遍，同选择器同优先级，
后一份把前一份逐条盖掉。改前面那份不会有任何反应，也不报错。

**为什么隐蔽**：语法完全合法，linter 默认规则不管，jsdom 只验「格子里有没有
那个数」而它确实有。只有逐字读那一行才看得出来。

**什么时候重现**：任何 `cond ? a : b` 里 a 和 b 长得很像的地方；
任何同一个选择器在一个文件里出现两次的地方。

**怎么防**：算出来的布尔值必须有一个分支产生可见差异，否则就别算。
❌ 没有测试盯着（jsdom 量不了视觉，见第 16 条）。

### 12d. 不带时区的时间戳会被读两遍

`utcnow()` 存的是 UTC，但 **SQLite 的 DateTime 列不保留 tzinfo**，读出来是
naive 的。`isoformat()` 给出 `2026-08-31T15:31:39`——不带偏移量——而
ES 规范规定，不带偏移量的 date-time 形式按**本地时间**解释。
于是 `new Date()` 把这个 UTC 时刻又当本地时间读了一遍：

```
库里 (UTC) 2026-08-31 15:31      实际生成于 23:31 本地
界面显示   2026-08-31 15:31      ← 东八区差 8 小时
```

凌晨生成的文章连**日期**都会退到前一天。

**为什么隐蔽**：三重的。差多少取决于用户在哪个时区；唯一的发现办法是
「记得自己到底几点点的生成」；而 **CI 跑在 UTC 上，差值是 0，测试永远绿**。

**什么时候重现**：任何把库里读出来的时间下发给前端的地方。
问一句：**这个字符串带偏移量吗？** 不带的话，两端各按自己的时区读一遍。

**怎么防**：下发前补回 UTC 标记（`models.as_utc`）。断言要盯「有没有偏移量」
这件事本身，不能去比时刻——比时刻的断言在 UTC 的 CI 上永远成立。
✅ 有测试：`test_web.py::test_下发的时间带时区标记`、`test_as_utc_对三种输入都不出错`

### 13. `em` 相对的是元素自身的字号

`max-width: 40em` 写在 19px 的正文上是 760px，写在 15px 的卡片上是 600px——
同一栏的两块左边缘差了 80px。栏宽这类要对齐的尺寸必须用绝对长度。

**什么时候重现**：任何用 `em` 定「栏宽」的地方。（`em` 定「和字号一起缩放的间距」是对的。）

❌ 没有测试盯着（jsdom 量不了，见第 16 条）。

### 14. 表格里贪心的一列会把其余列挤到竖排

目标词那列塞了一排 chip，把「删除」按钮挤成了「删/除」、「178 词 B2」断成三行。

**怎么防**：窄列加 `width: 1%; white-space: nowrap`（仓库里的 `.tight` 类）。

**什么时候重现**：任何新表格里出现一个内容可长可短的列。

### 15. 一次按键触发两件事

容器上和 `document` 上都挂了 `keydown`，容器那个只 `preventDefault()`
没 `stopPropagation()`。结果：焦点落在挖空词上按空格，
**词揭示了，同一下按键又把整个阅读器踢出了回忆模式**。

`preventDefault` 挡的是默认行为，不挡冒泡——这两件事经常被混为一谈。

**什么时候重现**：任何时候在容器和 `document` 上都挂了同类监听器。

✅ 有测试：`tests/frontend/dom.test.mjs`「空格揭示且不跳出回忆模式」

---

### 15b. 固定宽度的浮层 + 居中的正文，必然有一段视口区间会重叠

词条面板是 `position: fixed; right: 0; width: 430px`，正文是
`max-width: 760px; margin: 0 auto`。两个各自都对，凑一起就有一段区间必然重叠：
正文右缘 = `50vw + 380`，面板左缘 = `100vw - 430`，**视口窄于 1620px 就压上去**。
1440 的笔记本上压掉每行最后约 90px——正好是行末，读到那里眼睛要往回找。

**为什么隐蔽**：开发时窗口往往是最大化的（1920 上完全不重叠），而 jsdom
量不了布局（第 16 条），所以两道防线一道都没响。要发现它得**同时**满足
「窗口不是最大化」和「打开了词条面板」。

**什么时候重现**：任何「固定宽度的抽屉 / 浮层」配「居中的内容列」。
问一句：**这两个宽度加起来，在多宽的视口上才放得下？** 那个数以下全是重叠区。

**怎么防**：让内容让位，而且位移量按公式算（`clamp(0, 重叠量, 能让的最大值)`），
不要写「宽于某个值才让位」——断崖式阈值会让刚好卡在阈值下面的窗口一点不让，
而它明明还能让出一部分。
另外让位要用 `transform` 而不是改宽度：改宽度会重新折行、段落高度跟着变，
而 `lockHeights()` 锁的 `min-height` 是按旧宽度算的，一开面板整篇高度全错。
✅ 有测试：puppeteer 量 1920/1620/1440/1280/1100 五档的实际坐标
（jsdom 只验了 body 上那个类有没有打上）。

## 六、界面之外：这台机器上的边界

### 15c. 「只监听 127.0.0.1」挡不住 DNS rebinding

服务只绑回环地址，听上去已经够了。挡不住的是这一手：攻击者把自己的域名
解析到 127.0.0.1，**浏览器于是认为那是同源**——同源策略在这里不设防，
因为「同源」这个判断已经被 DNS 骗过去了。你访问的任意一个网页都能读走
整个文库和词库、删文章、以及发起生成把 API 额度烧掉。

**为什么隐蔽**：本机自测永远是正常的；`curl` 也永远是正常的。要复现得
同时具备「用户在访问别的网页」和「那个网页是坏的」两个条件，
而这两件事都不在这台机器的日志里。

**什么时候重现**：任何在本机开端口的程序。问一句：**除了「谁能连上」，
有没有校验过「对方以为自己在连谁」？**

**怎么防**：`TrustedHostMiddleware(allowed_hosts=["127.0.0.1", "localhost"])`。
代价是零——正常入口只有这两个名字，攻击者控制不了它们；
中间件自己会切掉 Host 里的端口，所以 `_pick_port` 换端口不受影响。
注意别为了测试把 `testserver` 加进白名单：那是在生产配置里开口子，
改 TestClient 的 base_url 才对。
✅ 有测试：`test_web.py::test_只认回环主机名`、`test_正常入口不受影响`

## 七、测试本身

### 16. jsdom 测不了任何视觉

它**没有布局引擎**：`offsetHeight` 恒为 0，`getBoundingClientRect()` 全是零，
**CSS 完全不参与**。所以前端那 107 个断言验的是 DOM 结构和事件流，
配色、间距、深色模式、响应式**一个像素都没验过**；
依赖真实布局的 `lockHeights()` 在 jsdom 里等于空跑。

**为什么危险**：测试全绿很容易被当成「界面没问题」。本会话第一版界面
jsdom 45/45 全过，用真浏览器一截图立刻发现三个可见 bug
（数据卡片错位 80px、表格列塌陷、每页一条 favicon 404）。

**怎么防**：改了 CSS 就自己开浏览器看一眼。不想装包的话，Chrome 自带的
`--screenshot` 就够用，一条命令出图：

```bash
chrome --headless --disable-gpu --hide-scrollbars --window-size=1440,1000        --virtual-time-budget=4500 --screenshot=out.png http://127.0.0.1:8000/
```

`--virtual-time-budget` 要给够——这几个页面的内容都是 JS 拉完接口才渲染的，
给少了拍到的是空壳。想看浅色主题加 `--blink-settings=preferredColorScheme=1`
（本项目没存过偏好时跟随系统）。要读 `getComputedStyle`、量实际宽高
再上 puppeteer-core 指向本机 Chrome。

### 17. 「因为你碰巧有数据所以过」的测试不算测试

前端夹具第一版直接读了开发机上的 `data/app.db`。本地有 7 篇文章所以全过，
CI 上克隆下来库是空的，`/api/articles/1` 返回 404，测试当场崩。

同类还有一条：断言写死了精确的超纲词集合，而那只在**下载过 CEFR-J 词表**时成立——
CI 上没有（运行时下载、已 gitignore），退回内置兜底表就多判了几个词。

**什么时候重现**：任何读真实库、真实配置、真实下载物的测试。

**怎么防**：夹具自己造数据；断言写「包含 / 不包含」这类真正要验的主张，
而不是写死一个只在某个环境下成立的精确值。

### 17b. 夹具太小的测试是空跑，不是通过

给「阅读位置记忆」写的第一版测试报告「存进 localStorage 的是 null」，
看着像功能坏了。实际是**夹具文章只有两句话，文档高度等于视口高度，
根本滚不动**——滚动事件一次都没触发，测试测了个寂寞。

反过来更危险：如果那条断言写的是「不该崩」而不是「该存进去」，
它会**通过**，而且以后一直通过，哪怕功能整个删掉。

**为什么隐蔽**：夹具是为了「内容固定、谁在哪跑都一样」（第 17 条）而造的，
而「小」正是让它固定的手段之一。没人会想到规模本身是个前提条件。

**什么时候重现**：任何依赖「内容超过某个量」才触发的行为——滚动、分页、
虚拟列表、懒加载、折叠、溢出滚动条。

**怎么防**：写这类测试时先断言**前提**成立（`scrollHeight > innerHeight`），
再断言行为。前提不成立就让它响亮地失败，而不是让主张静静地变成真空。

### 17c. 「不碰用户的数据」这条，隔离的范围要跟着状态走

`conftest.py` 顶上写着「不碰用户的库」，`temp_db` 也确实把 SQLite 换成了
临时文件。但本机的不可再生状态有**两份**：`data/app.db` 和
`config/settings.local.json`（装着 API Key）。接口层的 `client` 夹具只换了前者，
于是一条 `POST /api/settings` 的测试跑完，**用户存的用词上限就被改成了
测试里那个值**——本轮就是这么把 B2 改成 B1 的，而且没有任何地方会说一声。

**为什么隐蔽**：夹具看起来很完备，名字也叫 `client`；只有去数「这个应用一共
有几份持久状态」才会发现漏了一份。而且改坏的是设置，不是文章，
下次生成也不会报错——只是标尺悄悄换了一档。

**什么时候重现**：每新增一份持久状态，或者每新增一条会写它的接口测试。

**怎么防**：夹具按「这个应用有几份持久状态」来配，不是按「这条测试用到了什么」。
✅ 有测试：无法自测（测的是测试环境本身），但 `client` 夹具的 docstring
把两份状态都点名了。

### 18. 测试替身可能测的是替身自己的行为

生成中止的测试用 `TestClient` 时一直失败，换真实 uvicorn 就过了。
原因是 `TestClient` 不经过 socket，**客户端断开传导不到 `StreamingResponse`
的收尾逻辑**——测出来的是 `TestClient` 的行为，不是应用的。

**什么时候重现**：任何涉及连接生命周期、超时、断开、背压的测试。

✅ 现在这条用真实 uvicorn：`test_web.py::test_客户端断开时后台停下且不落库`

---

## 八、推理方法上的

### 19. 别拿一个有利的样本去验证一条要推广的规则

本会话我发现「19 个异干替补条目**全都**自身就是词表词条，
而 `ran`/`went`/`children` 一个都不是」，据此宣称**数据给出了干净的判据**。

往下核对才发现不成立：`was`/`is`/`had`/`did` 同样是词表词条，
而那些归并是对的。我拿一个恰好有利的子集，验证了一条不能推广的规则，
而且当时的语气比证据强得多。

**什么时候重现**：任何时候从数据里「发现」一条判别规则。
先去找反例，别只确认支持它的样本。

### 20. 两处实现同一件事，必然分叉

桌面版和网页版曾经并存，实现的是同一个产品。代价立刻就出现了：
`unplaced` 提示、四层检验、备份状态各写了两遍；
而词库页、每个词的命中标记只写了一遍，「停止」按钮又只写在另一边。

**漏掉的那一遍必然是后写的那端**，而且不会有任何东西提醒你漏了。

现在界面只有一套。**再出现「同一件事要写两遍」的结构时，先问能不能只写一遍。**

### 21. 借一个库之前，先分清你要的是它的代码、它的数据，还是它的判据

本轮扫同类开源项目，三次「要不要引入这个库」的答案完全不同，而区别不在库的
质量，在于**要的是它的哪一层**：

- **要代码** —— `json-repair`。它是个按 JSON 文法走的解析器，那套解析逻辑
  自己写不划算。<1MB、零传递依赖，直接进 `requirements.txt`。
- **要数据** —— LemmInflect。它的词形数据（源自 NIH SPECIALIST Lexicon）
  确实比规则法准，但本体 0.9MB 却要拖 **37MB 的 numpy**。
  所以跑一次脚本把数据筛出来提交进仓库（`scripts/gen_irregular.py`），
  运行期一个依赖都不加。
- **要判据** —— 同样是 LemmInflect，拿它的 13731 个屈折形态当**基准**，
  量出本仓库的漏认率是 3.43%，并据此定位到 `_suffix_candidates` 的长度门槛
  写宽了一格（第 6c 条）。这一层用完就扔，连数据都不用留。

**反过来最危险的一种**：把库的判断整个接过来。`getAllLemmas("better")` 返回
`good`——照单用它做还原，等于把第 5 条那个 bug 重新装回去。
**库的准确率是按它自己的目标衡量的，那个目标不一定是你的。**

**什么时候重现**：每次考虑引入一个新依赖。
先问：**我要的是它跑起来，还是它算出来的东西，还是只想拿它当尺子量一下？**
后两种都不需要把它装进产品。

### 22. 每一档都合理，不代表这个函数对输入有反应

`sizing()` 返回 (段数, 每段词数, 每段句数)。前两个跟着词数走，**第三个是常数**
（每段 5 句），段数那边还压着一条 `max(2, ...)` 的下限。三样凑一起的结果是：
**1 到 6 个词全都得到「2 段约 170 词」**，18 到 40 个词全都得到 510 词。
给一个词和给六个词读一样长的文章，多出来的全是稀释。

**为什么隐蔽**：没有哪一个输出是「错的」——170 词是一篇正常长度的文章，
2 段也合理。单看任何一次调用都挑不出毛病。要发现它得**连着试好几个不同的
词数、并且注意到提示文案一个字没变**。本轮就是用户这么发现的，不是测试。

**测试不但没拦住，还把它焊死了。** 原来那条写的是
`sizing(3)[0] == 2`、`sizing(1)[0] == 2` 这类写死的断言——它锁定的恰恰是
错误的行为，而且锁得很牢：改对之后这几条会失败，让人第一反应是「我改坏了」。
**写死具体值的测试，对「输出不随输入变」这类 bug 是反作用的。**

**什么时候重现**：任何把「输入 → 规格」映射成阶梯函数的地方——分页大小、
超时档位、重试次数、批量大小、布局断点。尤其注意其中被写成常数的那一维，
以及 `max()` / `min()` 钳住的边界：常数和钳位都不会报错，只会让函数对输入变得迟钝。

**怎么防**：断言**性质**而不是值——单调不减、不许出现长平台、输出与输入的
比值落在一个区间。这三条都不依赖具体数字，所以调参数时不用改测试，
而「不随输入变」会立刻挂。真有一段区间必须是平的（这里是舒适容量之外，
段数封顶后只能整句地跳），就把范围显式写进断言，而不是把阈值放宽。
✅ 有测试：`test_pipeline.py::test_篇幅跟着词数走`、
`test_每个目标词分到的篇幅是稳定的`、
`test_web.py::test_预览的篇幅跟着词数走`
