# 运行所需的全部依赖。run.bat 会自动同步这个文件。
#
# 界面是 web/ 那一套 HTML/CSS/JS，由 main.py 起一个只监听 127.0.0.1 的本地
# 服务，再用 Edge 打开。所以 fastapi 这几个不是「可选的 Web 版」，
# 是主路径上的依赖。
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
httpx>=0.27
pyyaml>=6.0
sqlalchemy>=2.0
# JSON 兜底解析的第 5 层（core/llm/jsonfix.py）。装不上也能跑——那一层会
# 自己关掉，只是少修几种畸形。<1MB、零传递依赖，所以放进主依赖而不是 dev。
# 它补的是前四层修不了的：结构位置上的中文引号、正文里没转义的引号和换行、
# 单引号、Python 字面量。这些都在真实模型输出里出现过。
json-repair>=0.30
# 背单词页的间隔重复调度（core/wordbook/scheduler.py）。装不上也能跑——
# 那一层会自己关掉，背诵退回「每轮把没背熟的全过一遍」。
# 间隔重复是个有正确答案的问题（DSR 模型，有论文有实测），自己拍一套只会更差，
# 而且差在哪儿看不出来：背单词的效果没有反馈回路，用户只会觉得「背了没记住」。
# MIT，纯 Python，唯一依赖 typing-extensions——和 json-repair 同一档。
fsrs>=6.0
