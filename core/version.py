"""版本号。**整个项目只有这一处**。

为什么值得单开一个文件：版本号天然是那种「要在好几个地方出现」的东西——
顶栏、接口、CHANGELOG、以后可能还有报错信息和问题反馈模板。
而这个项目已经吃过一次「同一件事写两遍必然分叉」的亏（需要注意.md 第 20 条），
版本号分叉的表现尤其难看：界面说 0.2.0、CHANGELOG 最新条目写着 0.3.0，
用户报 bug 时给的那个号对不上任何一次实际发布。

所以：**这里改一次，别处都跟着变**。别在模板里写死，别在 CHANGELOG 里
另起一套编号。`tests/test_version.py` 会把两处对不上变成一次测试失败。

编号规矩（语义化版本，但按这个项目的实际含义解释）：
  · 主版本 —— data/app.db 的结构发生了不兼容的变化，或者旧库需要人工介入。
    这是唯一真正要紧的那一档：库是不可再生资产。
  · 次版本 —— 加了功能，或者管线 / 界面有可感知的变化。
  · 修订   —— 修 bug、改文档、调样式。用户不会因为它改变任何用法。
"""
from __future__ import annotations

VERSION = "0.3.0"
