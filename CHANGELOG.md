# 更新记录

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

### 新增

- **词库页**：读过的词跨所有文章累计，可按「学习中 / 多语境 / 只见过一次 /
  已掌握」筛选、按词或释义搜索；点任意一个词看它在每篇文章里的原句和
  当时的线索审计结论。
- **L4 审计校准**：模型检验从三层加到四层。前三层验的都是「产出的形状对不对」，
  恰好把唯一要紧的能力漏在外面——这个产品的价值判定全压在语境线索审计
  那一次调用上，而它判错时用户察觉不到（能看出语境够不够的人本来就认识那个词）。
  L4 用同一段文本同时考一个强线索词和一个无线索词，见词就说 strong 的
  和见词就说 none 的会同时被拦下。
- **启动前自动备份**：`data/app.db` 的快照放在 `data/backups/`，只留最近 5 份，
  库没被写过就不重复留档。走 SQLite 自己的 backup API 而不是复制文件，
  源库损坏时直接报错，不会用坏数据覆盖好备份。
- **生成可中止**。「点停止」和「直接关页面」走同一条路，半篇文章不落库。
- 结果面板区分三种词：命中、本该写进去却没写、模型说塞不进因而没写。
- 测试套件：Python 219 项 + 前端 jsdom 78 项，以及 GitHub Actions。

### 变更

- **界面重做：把算出来的东西真正显示出来**。原先 CEFR 六个等级是同一个灰药丸、
  线索比是一个裸的 `3/5`、掌握程度是五个几乎看不见的灰点——数据都算了，
  但在界面上一律退化成「一个字符串」，扫一眼得不到任何结论。现在：
  - CEFR 配一套六级色阶（绿→青→蓝→紫→橙→红，色相单向推进，不会出现
    「B1 看着比 C1 难」的视觉倒挂），词库、文库、超纲词列表统一用；
  - 线索比画成分段条，满格和缺格在余光里就能分辨；老文章没这个字段，
    显示成「—」而不是「全空」——那是「没测过」，不是「一格都不达标」；
  - 词条面板里每处语境按当时的线索审计结论着色，一眼看出哪几处真能帮上忙；
  - **正文里的目标词改成红字**。原先只有一条 `--accent-soft` 的底色带，
    而那个值在深色下是 `#3b2a1c`，压在 `#1b1a22` 的阅读面上几乎看不出来——
    「哪几个是要学的词」得逐行去找，而那正是整个应用要你看见的东西。
    红色单开一个 `--target`，不复用 `--bad`：红在这里已经有「出错 / 无线索 /
    删除」的含义，而目标词是主角不是坏消息，两者会同屏出现（词条面板里
    「无线索」的语境 + 高亮的那个词），共用一个变量以后就再也分不开了。
    「高亮：关」连字色一起关掉；回忆模式的挖空和揭示后的词同色——
    它们是同一个东西，换色会让人以为「揭示」这个动作改变了什么。
  - 生成结果里列出**具体哪几个词线索不足、以及为什么**。那次审计调用是管线里
    最贵的一步，产出的逐词结论原先被压成一句「有 2 个词线索不足」，
    用户知道有问题却不知道该拿哪几个词去重生成。
- **生成过程有进度条和预计剩余时间**。原先只有一条时间线：它说得清「在干什么」，
  但答不了「还要多久」——而后者才是决定「等一下还是先去干别的」的那个信息，
  一篇要跑一两分钟。
  计量单位是**模型调用次数**而不是阶段数：调用是这条管线里唯一实质耗时的东西
  （一次 30-40 秒），而阶段数推不出调用数——修复和补线索是条件触发的，
  事先不知道会不会发生、发生几次。为此 `core/llm/client.py` 每发一次请求
  报一个 `call` 事件（重试也算，它一样在花时间）。
  「每次调用要多久」优先用本次已测到的，没测够就用 `/api/timing` 给的历史中位数
  （取自用户自己以前几篇）；两个都没有就**不显示时间**——宁可不说，
  也不要给一个编出来的数字让人据此安排。取中位数不取平均：偶尔一次重试或
  超长思考会把平均值拉飞，而那是「这次特别慢」不是「平时就这么慢」。
  三条不能违反的性质，都有测试盯着：条子只涨不退（往回缩会被读成出错）、
  跑完前封顶 98%（没跑完显示 100% 是撒谎）、调用飞行途中也要往前走
  （一次调用 30-40 秒里纹丝不动，看着就是卡死了）。
- **「用词上限」不再是四个没有含义的字母**。这一档决定整篇文章除目标词外能用
  多难的词，是生成页第二重要的选择，原先完全没有解释。现在四档各给：
  CEFR 官方能力描述、该档**及以下**的累计词汇量、四个例词（逐个对着
  `data/cefr.csv` 核过，都确实落在该级），点一行就切档。
  词汇量由 `/api/levels` 现算而不是写死在模板里——没下载 CEFR-J 时标尺会退回
  内置兜底表，写死的数字会和程序实际拦的东西对不上，而这种不一致
  用户没法自己发现（能判断「这篇的用词超没超 B2」的人本来就不需要这个功能）；
  此时改为不显示数字并说明原因。
  说明里给的建议是从机制推出来的，不是套话：**按自评水平往低选一档**——
  目标词本身已经是生词，其余的词必须是你认得的，语境线索才推得出词义；
  句子里再多一个不认识的词，那一处线索就废了。
- **首页不再是一张孤零零的输入卡**。宽屏上四分之三是空的，而且生成要跑一两分钟，
  中间发生什么完全没交代。现在右栏常驻「它会做四件事」和「最近生成」，
  输入卡下面给两组能直接点的示例词。
- 列表页铺开到 1440（原先 1180，宽屏上大片空白）；阅读页仍收在 `--measure`，
  那里窄才是对的。
- 深色主题拉开层次：页面底与卡片原先只差 3% 明度，卡片和背景几乎分不开。
- 表格行里的「删除」原先是每行最亮的元素，而它恰恰最不该被先看到；
  改成 hover 才显形。

- **界面合并成一套，跑在 Edge 里**。原先桌面用 PySide6 控件另画了一份 UI，
  和网页版实现的是同一个产品，每加一个功能要写两遍。现在只保留 `web/` 那套
  HTML/CSS/JS：`main.py` 起本地服务，再用 Edge 打开。用法不变（双击 `run.bat`），
  但那个窗口现在是浏览器，不是应用窗口。
  删掉 `ui/` 约 1900 行，以及中间过渡用的 Qt 外壳 `app.py`；
  依赖里去掉 PySide6（约 200 MB），首次安装和启动都快了。
- **`unplaced` 真正生效**：模型判断塞不进的词不再被硬塞回最后一段，
  而是如实报出来。硬塞会把文章变成填空作业，反而破坏语境记忆；
  而用户看到的却是「生成完成」，没有任何线索说明哪里不对。
- 前端重做：设计令牌集中在 `web/static/css/tokens.css`，页面由
  `web/pages.py` 的注册表驱动（加页面不用改 `main.py` 和 `base.html`）。
- 一次最多接受的词数从 Web 路由搬到任务层，两个入口不会再分叉。

### 修复

- **四层检验自己会崩**。L3 拿到模型返回的顶层数组时直接 `doc.get()`，
  抛出的 `AttributeError` 不在 `except` 的捕获范围里，一路冒到接口层变成
  HTTP 500——用户看到「检验请求失败」，而不是「任务验收没过、原因是顶层
  不是对象」，而且最要紧的 L4 一次都跑不到。检验在它该报告问题的那一刻
  崩掉，等于这一层不存在；而顶层数组正是管线本来就救得回来的形状之一。
- **删文章的计数只修了原因，没修数据**。`delete_article` 改成按事实重算之后，
  在那之前用旧代码删掉的文章留下的虚高计数不会自己好——重算只在该词条
  又被别的删除路径碰到时才发生，碰不到就一直错下去（开发机的库里 24 个
  词条有 5 个对不上，`abandon` 显示 10 次、实际只有 3 处语境）。
  现在启动时按事实校一次，只写对不上的行；**动手之前强制留一份快照**——
  `backup.run()` 原本会因为「库自上次备份后没被写过」而跳过，而那恰好就是
  这次要动库的场景，等于唯一一次真正改数据的启动没有当次备份兜底。
- **端点返回的不是 JSON 时抛的是 `ValueError`**。`base_url` 指到网页或代理上
  时拿回来的常是一个 200 的 HTML 页，而 provider 这一层对外的约定是
  「任何失败都变成带上下文的 `ProviderError`」——`r.json()` 是这条约定上唯一
  的漏洞，漏出去的异常同样变成 HTTP 500，偏偏就发生在专门用来诊断这类
  配置错误的那个页面上。
- **超纲检测漏掉每句的第一个词**。原先「大写就跳过」想跳专有名词，
  副作用是模型只要把生词放在句首就能绕开整把难度标尺。现在专有名词由
  选题阶段声明（plan 多返回 `names`），句首词因此可以无条件检测。
- **模型返回「合法但形状不对的 json」时整篇作废**。`json_schema` 只有 Kimi
  会真的执行，DeepSeek 收下也不生效，所以顶层数组、`words: null`、
  `strength` 是数字这类是常态。共 10 处会抛 `AttributeError`，
  而那时已经花掉几分钟和上万 token。现在统一先归一形状。
- **词形还原把别的词判成目标词**。判据方向错了：`better` 的词典父词条是 `good`，
  于是文中出现 `good` 就被判成「目标词已出现」，`good` 还会被当成 `better`
  的形态写进词条库。
- **删文章后 `times_seen` 不回退**，词条面板会显示「见过 3 次」却只列得出
  1 处语境，文库页的「在多个语境中见过」永久虚高。
- **模型往选题里自己加词**会白烧掉整段的修复预算——那个词不在 allow 里，
  校验一边要求它出现、一边把它判成超纲词，两条指令互相打架。
- 回忆模式点「全部揭示」会漏掉正在显示中文的句子里的词，切回英文后仍是空格。
- 焦点在挖空词上按空格会同时揭示该词并跳出回忆模式。
- 双击 `run.bat` 时 uvicorn 因 `sys.stdout is None` 直接抛，异常死在后台线程里，
  表现成「端口一直起不来」。根因是用 `pythonw` 无控制台启动，
  而这个只在双击时出现，用 `python` 跑是好的。现在不再隐藏控制台。
- 掩码后的 API Key 回传时会覆盖真 key：判据写成了「以 `*` 开头」，
  而 `mask()` 产出的是 `sk-ab********wxyz`，开头恰恰不是 `*`。
- 指定别家模型时会把 A 家的模型名发给 B 家的端点。
- 文库表格窄列被目标词那列挤到竖排（"删除"变成"删/除"）。
- 阅读页数据卡片与正文左边缘差 80px：`40em` 的 `em` 相对元素自身字号，
  19px 的正文和 15px 的卡片算出两个宽度。
