# 给 AI 助手的项目说明

改这个仓库之前先读这份。README 讲的是「怎么用」，这里讲的是「改的时候要注意什么」。

## 这个产品到底在做什么

一句话：**保证每个目标词出现时，上下文足以让人推出词义**。

不是「让 AI 造几个例句」。词出现了但读者猜不出词义，这一遍就等于白读，
跟单词书没区别。所有其它东西——难度标尺、句级对齐、四种阅读模式、存储——
都是为这一件事服务的手段。

由此有两条贯穿全项目的判断依据：

**一、用户无法自己验证核心质量。** 能判断「这个语境够不够推出词义」的人，
本来就已经认识这个词了。所以这类错误**没有反馈回路**——不像崩溃或乱码，
你永远等不到用户来报。它不会自我纠正，损害对时间积分无上界。

推论：**排查和修复时，按「沉默度 × 永久性」排序，而不是按「看起来多严重」。**
在这个项目里响度和损害是负相关的——会崩会报错的 bug 自带反馈回路，
总损害被「被发现的时间」截断；用户察觉不到的没有。

推论二：**任何时候都要把沉默的失败换成可见的失败。** 比如模型说某个词
塞不进这个题材，宁可少写一个词并如实标出来，也不要硬塞进去交一篇稀释过的
文章——后者用户看到的是「生成完成」。

**二、`data/app.db` 是唯一不可再生的资产。** 文章能重新生成，
掌握程度和累计语境不能，它们是一次次阅读攒出来的。动存储相关的代码时，
先想清楚出错会不会把这份资产变脏，以及能不能恢复。

## 结构

```
core/     业务地基，不依赖上层
  provider/  模型接入（OpenAI 兼容；各家偏差用 Capabilities/Quirks 显式声明）
  llm/       调用、重试、JSON 四层兜底解析
  lexicon/   CEFR 分级、词形还原、超纲检测
  store/     SQLite + 启动前快照
  health.py  四层正确性检验
tasks/    「一件能对着词表做的事」。新增功能在这里加子类，core/ 和 web/ 不用改
web/      唯一一套界面。pages.py 是页面注册表，加页面改这里一行
app.py    桌面外壳：起本地服务 + Qt 窗口内嵌 Chromium
main.py   纯服务入口，想用浏览器时跑这个
```

依赖方向单向：`web/` → `tasks/` → `core/`。`core/` 里不要 import 上层。
唯一的例外是 `core/health.py` 的 L4，它必须 import 任务层真正在跑的那段 prompt——
在 health.py 里另抄一份就变成「校验通过但实际审计仍然失灵」。

## 改动时容易踩的坑

下面是最常撞上的几条。**完整清单在 [需要注意.md](需要注意.md)**——
那里按「什么改动会让它重现」组织，每条都标了有没有测试盯着。

**模型返回的 json 形状不可信。** `json_schema` 只有 Kimi 会真的执行，
DeepSeek 收下也不生效。任何 `llm.json()` 的返回值都要先过
`tasks/article/schema.py` 的 `coerce_*`，不要直接 `.get()`。
形状错误一路带到下游抛出来的是 `AttributeError`，
而那时整篇文章已经花掉几分钟和上万 token。

**推理模型的 `max_tokens` 含思考 token。** 预算给不足，思考会把正文挤成空字符串。
这就是 DeepSeek 文档里「偶尔返回空 content」的根因。预算是上限不是预留，
开大不额外花钱。见 `config/providers.yaml` 的 `reasoning.headroom`。

**模型名一律走配置，不进代码。** `kimi-k2.5` 和整个 `moonshot-v1` 系列
已于 2026-08-31 下线；把模型名写死的项目当天全线报错。

**`pythonw` 下 `sys.stdout` 是 `None`。** 任何 print、任何往 stdout 装
handler 的库都会当场炸（uvicorn 就是）。而这正是双击 `run.bat` 的真实路径——
**只用 `python app.py` 测是测不出来的**。

**改 CSS 之后 jsdom 测试给不了任何保证。** 它没有布局引擎，
`offsetHeight` 恒为 0，CSS 完全不参与。视觉改动要自己开浏览器看，
或者用 puppeteer-core 指向本机 Chrome 截图。

## 跑测试

```bash
pip install -r requirements.txt -r requirements-dev.txt
pytest                                  # Python，191 项

cd tests/frontend
npm install && python make_fixtures.py && npm test    # 前端，61 项
```

测试全部离线：涉及模型的用 `conftest.py` 里的 `FakeLLM`，返回什么由测试决定。
真去调模型的测试跑不稳、要花钱，而且验不了「模型返回畸形数据时会怎样」——
而那恰恰是这个项目最需要覆盖的部分。

写新测试时，优先覆盖「错了也不会报错、只会安静给出错结果」的那一类。

## 风格

- 注释写**为什么**，不写做了什么。尤其是那些「看起来可以更简单」的地方，
  要说明为什么没那么写——多数都是踩过的坑。
- 中文注释和中文测试名是这个项目的既定风格，保持一致。
- 加组件写进 `web/static/css/app.css`，别在模板里写内联样式；
  任何色值走 `tokens.css` 的变量。
